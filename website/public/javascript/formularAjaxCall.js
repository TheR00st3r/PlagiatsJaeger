// $(document).ready(function() {
	// $('#saveAjaxCall').click(function() {
		// $.post('ajax.php', {
			// formular : $("#myForm").serialize()
		// }, function(data) {
			// if(data == 0) {
				// toastr.success("Änderungen gespeichert.");
			// }
			// else {
				// toastr.error("Es ist ein Fehler beim Speichern aufgetreten.");
			// }
		// });
	// });
// });
